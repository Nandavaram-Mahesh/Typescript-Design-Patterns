// import {crypto} from "crypto";

class PasswordHasher {
  hash(password: string): string {
    // return crypto.createHash("sha256").update(password).digest("hex");
    return "calfndasnafvakjnasfnasf"
  }
}

export {PasswordHasher}
